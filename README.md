# Post-Notification-Automation-for-AkashDTH-cms-Playwright
A Playwright-based CMS automation tool that bulk uploads image assets, automatically retrieves the generated CDN URLs from the admin panel, and exports them into a structured text file for downstream use.
